# Frontend

Minimal React UI for:
- Register + login
- Protected dashboard
- Task CRUD (create/list/update/delete)
- API success/error messages

By default, it calls `http://localhost:3000/api/v1` and can be overridden with:

```bash
VITE_API_BASE_URL=http://localhost:3000/api/v1
```
